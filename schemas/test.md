# Schemas test file

This file is in the "strict" ruleset scope. PRs touching this path should require code owner approval before merging.

## Test 3 edit
Attempting self-merge of a PR touching only this path. Expected: merge blocked because @JefCurtis is the sole code owner and can't self-approve.
