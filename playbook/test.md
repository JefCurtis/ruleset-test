# Playbook test file

This file is in the "loose" ruleset scope. PRs touching only this path should allow self-merge without approval.

## Test 2 edit
Attempting self-merge of a PR touching only this path. Expected: merges with no approval required.
